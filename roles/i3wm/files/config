# i3 config file (v4)
# Documentation: https://i3wm.org/docs/userguide.html

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).

###############################
# Basic Configuration
###############################
set $mod Mod4
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Source Code Pro 10
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex-autostart --autostart --environment i3

###############################
# System Utilities
###############################
# Lock screen before suspend
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork --color 000000
# NetworkManager system tray GUI
exec --no-startup-id nm-applet
# Bluetooth applet
exec_always --no-startup-id blueman-applet
# Clipboard Manager
bindsym $mod+v exec "clipmenu -i -fn 'Source Code Pro-14' -nb '#282828' -nf '#C5C8C6' -sb '#F0C674' -sf '#282828'"


###############################
# Power Profile Control
###############################

###############################
# Sound and Volume Control
###############################
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

###############################
# Workspace Configuration
###############################
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Workspace switching
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

###############################
# Window Management
###############################
# Start a terminal
bindsym $mod+Return exec alacritty

# Kill focused window
bindsym $mod+q kill

# Split in horizontal orientation
bindsym $mod+c split h

# Split in vertical orientation
bindsym $mod+x split v


# Fullscreen mode toggle
bindsym $mod+f fullscreen toggle

# Change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split
# Toggle between layout modes
bindsym $mod+w layout toggle all

# Toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Focus the parent container
bindsym $mod+a focus parent

###############################
# Focus Windows
###############################
# Focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# Move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

###############################
# Resize Windows
###############################
# Resize windows directly
bindsym $mod+Control+h resize shrink width 5 px or 5 ppt
bindsym $mod+Control+j resize grow height 5 px or 5 ppt
bindsym $mod+Control+k resize shrink height 5 px or 5 ppt
bindsym $mod+Control+l resize grow width 5 px or 5 ppt

###############################
# Screen Brightness Controls
###############################
bindsym XF86MonBrightnessUp exec light -A 5
bindsym XF86MonBrightnessDown exec light -U 5

###############################
# Screenshot
###############################
bindsym $mod+Shift+s exec "maim -s | tee ~/Downloads/$(date +%Y-%m-%d_%H%M%S).png"

###############################
# Status Bar Configuration
###############################
#bar {
#    status_command i3status
#    position top
#    tray_output eDP-1
#}
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# event listener for our layout indicator module
exec --no-startup-id ~/.config/polybar/i3-layout-event-listener.sh

###############################
# Appearance Settings
###############################
# Window titles
for_window [all] title_format "%title"
# Window icons and borders
for_window [all] title_window_icon padding 3px
#for_window [all] border normal 4
for_window [all] border pixel 1
# Gaps
gaps inner 8
# Colors
# class                 border      backgr.    text      indicator child_border
#client.focused          #F0C674     #282A2E    #F0C674   #8ABEB7   #F0C674
client.focused          #F0C674     #282A2E    #F0C674   #F0C674   #F0C674
client.focused_inactive #373B41     #707880    #C5C8C6   #484e50   #373B41
client.unfocused        #282A2E     #282A2E    #888888   #292d2e   #282A2E
client.urgent           #A54242     #900000    #ffffff   #900000   #900000
client.placeholder      #707880     #0c0c0c    #ffffff   #000000   #0c0c0c
client.background       #282A2E

###############################
# Mode Configurations
###############################
# Resize mode
#mode "resize" {
#    bindsym h resize shrink width 5 px or 5 ppt
#    bindsym j resize grow height 5 px or 5 ppt
#    bindsym k resize shrink height 5 px or 5 ppt
#    bindsym l resize grow width 5 px or 5 ppt
#    bindsym Return mode "default"
#    bindsym Escape mode "default"
#    bindsym $mod+r mode "default"
#}
#bindsym $mod+r mode "resize"

###############################
# Custom Commands and Setups
###############################
# Screen setup script
exec_always --no-startup-id ~/.config/i3/scripts/xrandr_dock_setup.sh

# Flatpak available in dmenu script
exec_always --no-startup-id ~/.config/i3/scripts/add_flatpaks_to_dmenu.sh

# Custom window behavior for ChatGPT
#for_window [title="^ChatGPT -"] floating enable, resize set 652 1200, move position 0 0

###############################
# System and i3 Controls
###############################
# Reload the configuration file and restart i3 in-place
bindsym $mod+Shift+r restart
bindsym $mod+Shift+c reload

# Exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

###############################
# Misc Configurations
###############################
# Drag floating windows to their wanted position
floating_modifier $mod

# Program launcher (dmenu replacement is rofi)
# bindsym $mod+d exec "rofi -modi drun,run -show drun"
# dmenu is the default program launcher
#bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+d exec --no-startup-id dmenu_run -i -fn 'Source Code Pro-14' -nb '#000000' -nf '#C5C8C6' -sb '#F0C674' -sf '#000000'


# Move tiling windows via drag & drop (title bar or floating modifier)
tiling_drag modifier titlebar


###############################
# Workspace Profiles
###############################
# Workspace 1 layout and applications
#exec_always --no-startup-id i3-msg 'workspace 1; append_layout .config/i3/workspace-profiles/workspace_1_layout.json'
#exec --no-startup-id i3-msg 'workspace 1; exec chatgpt'
#exec --no-startup-id i3-msg 'workspace 1; exec konsole'


# Always have Thunderbird on his own workspace
for_window [class="thunderbird"] move to workspace $ws9


###############################
# Recommendations (Optional)
###############################
# Consider organizing your custom scripts and frequently used commands
# into separate script files for better manageability.
#
# Example for a custom script to launch frequently used applications:
# exec_always --no-startup-id ~/.scripts/my_startup_apps.sh
#
# Utilize i3's 'assign' feature to automatically send specific applications
# to designated workspaces, improving workspace organization.
#
# Example to assign Firefox to workspace $ws2:
# assign [class="Firefox"] $ws2
#

###############################
# End of Configuration
###############################
# Remember to make your configuration executable if including any scripts:
# chmod +x ~/.config/i3/config
#
# Reload your configuration with $mod+Shift+r to apply changes.
